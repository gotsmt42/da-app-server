# multer-storage-cloudinary@4 ประกาศ peer เป็น cloudinary@^1 แต่โปรเจกต์ใช้ cloudinary@2
# (ใช้งานได้จริง API ส่วนที่มันเรียกไม่เปลี่ยน) — ถ้าไม่ตั้งค่านี้ `npm install` เปล่าๆ จะล้มด้วย ERESOLVE
# ตั้งไว้ที่นี่เพื่อให้ทั้งเครื่อง dev และ build server (Render) ทำงานได้เหมือนกันโดยไม่ต้องจำ flag
legacy-peer-deps=true
